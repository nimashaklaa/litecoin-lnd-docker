# Litecoin LND Node Configuration
# Copy this file to .env and fill in your values

# ===========================================
# LITECOIND CONNECTION SETTINGS
# ===========================================

# Your litecoind RPC host (e.g., your-server.example.com:19332)
LITECOIND_RPCHOST=your-server.example.com:19332

# RPC username for litecoind
LITECOIND_RPCUSER=your_rpc_username

# RPC password for litecoind
LITECOIND_RPCPASS=your_rpc_password

# ZMQ endpoints for raw blocks and transactions
# These must match what's configured in your litecoind
LITECOIND_ZMQPUBRAWBLOCK=tcp://your-server.example.com:28333
LITECOIND_ZMQPUBRAWTX=tcp://your-server.example.com:28332

# ===========================================
# LND SETTINGS
# ===========================================

# Your node's alias (visible on the network)
LND_ALIAS=my-litecoin-lightning-node

# Your external IP address (for other nodes to connect to you)
# Leave empty if behind NAT or not accepting incoming connections
LND_EXTERNALIP=
